Studentlist = getElementsByClassName(student-List)

const showPage = showPage(list, page)

const ListToPage = list.appendChild(page) //We append the list to the page
var pageNumber = page + 1 // A variable that can go to the next page? Not sure here

    for(var i = 0; i < List.length; i++) 
    ListToPage
    if {
      StudentlistList <= 10;
      return pageNumber;
}
